# This file contains example environment variables for the backend application.
# It serves as a template for the actual `.env` file, which should contain
# sensitive information and should not be committed to version control.

# PostgreSQL Database Configuration
# These variables define the connection details for the PostgreSQL database.
POSTGRES_SERVER=db
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=backend
# The full database connection URL. This is typically used by SQLAlchemy.
DATABASE_URL=postgresql://user:password@db/backend

# JWT (JSON Web Token) Settings
# These variables are used for generating and validating JWTs for authentication.
# SECRET_KEY: A strong, random secret key used for signing the JWTs.
#             It must be at least 32 characters long and kept confidential.
SECRET_KEY=YOUR_VERY_SECRET_KEY_THAT_IS_AT_LEAST_32_CHARS_LONG
# ALGORITHM: The cryptographic algorithm used for signing the JWTs (e.g., HS256, RS256).
ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES: The expiration time for access tokens in minutes.
#                              (e.g., 60 minutes * 24 hours * 8 days = 11520 minutes for 8 days).
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Redis Configuration
# This variable defines the connection URL for the Redis server,
# which is typically used for caching and as a message broker for Celery.
REDIS_URL=redis://localhost:6379