# This file specifies intentionally untracked files that Git should ignore.
# These files are typically generated by the build system, development tools,
# or contain sensitive information, and should not be committed to the repository.

# Ignore virtual environment directories.
# These directories contain Python packages specific to a project and are usually
# recreated from requirements files.
.venv/
venv/
ENV/

# Ignore Python-specific cache and build directories.
# These are generated during development and testing.
__pycache__/
.pytest_cache/
.ruff_cache/

# Ignore Node.js dependency directory and log files.
# `node_modules` contains installed packages and can be very large.
node_modules/
npm-debug.log
yarn-error.log

# Ignore editor-specific directories and files.
# These are typically user-specific configurations or temporary files.
.idea/
.vscode/
*.swp

# Ignore OS-generated files.
# These are system-specific files that are not part of the project's source code.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Ignore build artifacts.
# These are generated during the build process and can be recreated.
build/
dist/
*.egg-info/

# Ignore log files.
*.log

# Ignore coverage report files.
# These are generated by code coverage tools.
.coverage
coverage.xml
htmlcov/

# Ignore sensitive environment variable files.
# `.env` files often contain API keys, database credentials, and other secrets.
# `!.env.example` explicitly includes the example file, which is safe to commit.
.env
.env.*
!.env.example

# Ignore MLflow tracking server artifacts.
# These are generated by MLflow during machine learning experiments.
mlruns/

# Ignore large data, model, and database files
*.db
*.pth
*.enc

# Ignore specific generated files
test_warnings.txt
.next/
frontend_tests_output.txt
playwright-report/
test-results/
db_custom/
secure_storage/
